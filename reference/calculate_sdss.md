# Calculate the differential drug sensitivity score (DSS)

Reference the DSS to the mean of a (healthy) control cohort

## Usage

``` r
calculate_sdss(dss, mean_dss_ctrl)
```

## Arguments

- dss:

  Drug sensitivity score

- mean_dss_ctrl:

  Average drug sensitivity score of the control cohort

## Value

Differential drug sensitivity score
