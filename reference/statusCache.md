# Status of R.cache

Enquire the status of caching and return the number of key files from
[`R.cache::getCachePath()`](https://rdrr.io/pkg/R.cache/man/getCachePath.html).

## Usage

``` r
statusCache()
```
